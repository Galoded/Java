<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 通过mapper接口默认配置，将mapper.xml和mapper.java放到同一个包下面 即可-->
	<!--加载映射文件 -->
	<!-- <mappers><mapper resource="mybatis/sqlMapper/User.xml" /> <mapper 
		resource="mybatis/sqlMapper/UserMapper.xml" /> </mappers> -->
</configuration>